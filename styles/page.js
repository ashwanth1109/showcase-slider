import { w, m, flex, pos } from "./style";

// ------------------------------------------------------------
// index
// ------------------------------------------------------------
export const index = {
    container: {
        ...w.w100p,
        ...w.mw1000,
        ...m.hAuto,
        ...flex.row,
        ...pos.justifyEvenly
    }
};
